type Tarefa @model @auth(rules: [{allow: public}]) {
  id: ID!
  titulo: String!
  infor: String!
  IsComplete: Boolean!
}

type FormTeste1 @model @auth(rules: [{allow: public}]) {
  id: ID!
  nome: String!
  infor: String!
  tipoteste: String!
  descricao: String!
  finalizado: Boolean!
  bddLongo: String!
}
 